<h1>
  <%= @question.title %>
</h1>
  <div class="small-3 columns">
    <div class="small-12 columns">
      <%= @question.description %>
    </div>
  </div>
  <thead>
    <tr><th>Answers for <%= @question.title %></th></tr>
  </thead>
  <% @answers.each do |answer| %>
 <tr>
   <td>
     <%= answer.description %>
   </td>
 </tr>
<% end %>
  <%= render 'answers/form' %>

<%= link_to 'Back to index', root_path %>
